ORIGINAL WORD DOCUMENTS

These are the editable originals (Word, Markdown and HTML) of the resources. They are NOT published on the website.
The site publishes the PDF copies in resource-files/.

To update a resource: edit the Word file here, save it as PDF (File > Save As > PDF),
and replace the PDF in the matching resource-files/ folder, keeping the same file name.
